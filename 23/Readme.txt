Sudoku Game
1.Designed UI/UX, like text align center.
2.Completed the check function to check all rows, all cols and 3x3 cells. Highlight with red color, if find a repeated number. 
3.The check function executed automatically after each textChanged event.
4.If user complete the table correctly and win the game, shown a message box. 🎉
5.If an error occurs while loading txt files, handled the error.
6.Added Solve menu, and use py-sudoku package to shown the solved table to user.